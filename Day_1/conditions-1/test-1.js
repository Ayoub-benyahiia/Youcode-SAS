const prompt = required('prompt-sync')();
let N = +prompt("entre your nombre of artciles");

let total =
let i = 0;
while (N <= i) {
    // let i = 1;
    if (N = 0) {
        console.log("total est 0")
    }
    else if (N = 1) {
        console.log("total = 125 et aucune remise")
    }
    else (){
        console.log("total")
    }
}